import XCTest
@testable import SwiFtySymbolsShared

final class SwiFtySymbolsSharedTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiFtySymbolsShared().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
